{
  "name": "gst-itr-webhook-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "update-tunnel": "echo 'Run: npx wrangler secret put TUNNEL_ORIGIN'"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
